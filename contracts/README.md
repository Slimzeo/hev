# Contracts

This directory contains versioned, machine-readable contracts shared across the Go core and host adapters. [`cli/v2/`](./cli/v2/) is the current hev CLI process protocol; adapters must reject unsupported schema versions instead of falling back to v1.

