# OpenCode adapter

This adapter is planned but not implemented. A future implementation should reuse the versioned HEV CLI contract and keep OpenCode-specific runtime and Skill models inside this directory.

