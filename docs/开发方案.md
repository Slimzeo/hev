## deepseek-harness 接入

### 底层能力思路

四个SOLUTION：


1. dsh-tool-skill层，
   ```typescript
   
       signal.throwIfAborted()
       if (!snapshot.complete) return decision
       //SOLUTION1: 直接在这里对snapshot.skills进行过滤，过滤掉不符合InCurrentEnv的skill
       const skills = snapshot.skills.filter(isModelInvocable)
       const entries = catalogSourceEntries(skills, catalogDescriptionMaxLength)
       const digest = digestCatalogEntries(entries)
       const history = catalogHistory(agent)
       const existing = catalogMessage(decision.messages)
       if (history.visibleDigest === digest) {
         return existing === undefined
           ? decision
           : { kind: 'enter', messages: decision.messages.filter(message => message.id !== existing.message.id) }
       }
       if (existing !== undefined && digestCatalogEntries(existing.entries) === digest) return decision
       if (!history.published && skills.length === 0) {
         return existing === undefined
           ? decision
           : { kind: 'enter', messages: decision.messages.filter(message => message.id !== existing.message.id) }
       }
   ```

   

2. SkillRegistry, dsh-skill层
   ```typescript
   
     /**
      * Observe the current invocation-neutral catalog and whether discovery completed within a stable revision.
      * Incomplete observations are never cached, allowing consumers to retain last-good state and
      * retry on their next request boundary.
      * @param options - view options; `scope` selects the viewing agent's layers, `cwd` selects project roots, and `signal` cancels discovery.
      * @returns sorted summaries plus discovery-completeness state.
      */
     async snapshot(options: SkillViewOptions = {}): Promise<SkillCatalogSnapshot> {
       const collected = await this.collect(options)
       //SOLUTION2: 在这里对collected.entries进行过滤，过滤掉不符合InCurrentEnv的skill
       return {
         skills: [...collected.entries.values()]
           .map(entry => toSummary(entry.candidate))
           .sort(compareSkillSummary),
         complete: collected.cacheable,
       }
     }
   
   ```

   

3. Dsh-skill-filesystem, skill-filesystem层
   ```typescript
     type SkillProvider,
     type SkillProviderControl,
     type SkillProviderObservation,
     type SkillSource,
   } from '@deepseek-ai/dsh-skill'
   
   const PROJECT_DSH_RANK = 100
   const PROJECT_AGENTS_RANK = 200
   const CUSTOM_RANK = 300
   const USER_DSH_RANK = 400
   const USER_AGENTS_RANK = 500
   const DEFAULT_WATCH_STABILITY_THRESHOLD_MS = 200
   const DEFAULT_WATCH_POLL_INTERVAL_MS = 100
   const DEFAULT_WATCH_MAX_PROJECTS = 128
   
   //SOLUTION3: 维护一个envsystem的逻辑地址概念，叠加env层面的provider root entries召回
   
   
   export const name = 'skill-filesystem'
   export const inject = ['skills']
   
   /** Local filesystem skill provider configuration. */
   export interface Config {
     /** Uni
   ```

4. 直接修改agent-preset，禁用skill-filesystem
相应的：hev plugin的实现也改为对于每个env新建一个agent-preset，并且需要知道skill的绝对路径来进行加载，需要在插件层补充SkillProvider对接@dsh-skill
官方预制的四种在deepseek-harness/apps/cli/config/agent-presets，个人自定义的放在.dsh/agent-presets 下
- 好处：
1. 后续也能同时在这里管理其他预设如MCP
2. 天然支持会话重启时加载特定的环境
3. 可以自然切回官方预设
- 坏处：
一个session只能在说话之前就确认选择一个env
```
  - id: skill-filesystem
    name: '@deepseek-ai/dsh-skill-filesystem'
    disabled: true
```

5. 同样修改agent-preset，但是设置禁止读取默认目录
这条路径需要对每个环境都存一份物理skill源
```
  - id: skill-filesystem
    name: '@deepseek-ai/dsh-skill-filesystem'
    config:
      providerName: hev-environment
      includeDefaultRoots: false
      customSkillDirs:
        - /absolute/path/to/skills-root
```





### env runtime 维护思路

referrence：

- conda -> shell.temp_env_variable(CONDA_ENV)
  每次activate写入CONDA_ENV进shell来获取对应环境
  维护一个model来配置修改对应的自己进程状态
  所以不同terminal能有不同环境覆盖而相互隔离

- dsh-session runtime -> 进程内存里面，维护两个Map
  AgentRegistry：SessionId -> Agent

  ```typescript
  export class AgentRegistry extends Service {
  	private store = new Map<SessionId, AgentEntry>()
  }
  //
  const agent = ctx.agents.get(sessionId)
  interface Agent {
    id: SessionId
  	options: AgentOptions
  	session: Session
  	inbox: Inbox
    status: 'idle' | 'running'
    ctx: Context
  }
  ```

  另一个内存Map：

  ```typescript
  export class SessionStore extends Service {
    private store = new Map<SessionId, SessionEntry>()
  }
  //
  const session = ctx.sessions.get(sessionId)
  
  ```

  DSH session状态：
  当前runtime ： 从Map里面取
  持久： jsonl

  怎么取得当前Session Runtime？
  —— 根源上都是通过sessionId去get相应的session models

  那么sessionId为什么在当前会话里面会被保存？怎么保存的？
  —— 答：不管是open code还是dsh，只要在当前session 内，就会对所有内部命令传递sessionId。
  我们顺应这个框架拿到sessionId即可

  

solution：

- 所以获取environment-runtime关键在于：
  同样维护一个`EnvironmentEntry`:

  ```go
  var sid SessionId
  var eid EnvironmentId
  ```

  然后通过eid获取到environment的具体信息，来对skill进行“过滤”
  **但是过滤的具体逻辑怎么做？**
  **操作系统怎么通过逻辑地址获取相应物理地址的？**

### 具体过滤策略



